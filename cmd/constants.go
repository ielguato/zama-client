package cmd

const (
	serverBaseURL = "http://localhost:8080" // Base URL for your server
	downloadDir   = "downloads"             // Directory for storing downloads
	segmentsDir   = "segments"              // Directory for storing file segments
	chunkSize     = 32                      // Size of the file chunk
)
